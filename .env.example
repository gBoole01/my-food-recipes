# Root-level reference only — each app also has its own .env.example.

# Postgres connection string used by apps/api.
# Local dev (docker-compose): postgresql://postgres:postgres@localhost:5432/food_recipes
# Production: the connection string provided by Supabase.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/food_recipes
